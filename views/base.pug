doctype html
html
    head
        block head
            meta(charset='UTF-8')
            meta(name="viewport" content="width=device-width, initial-scale=1.0")
            link(rel='stylesheet' href="https://fonts.googleapis.com/css?family=Lato:300,300i,700")
            link(rel='stylesheet' href='/css/style.css')
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
            title Natours | #{title}
        //- title Natours #{tour} 
        //- interpolation similar to es6 template strings except use # instead of $
    //- body
    //-     h1= tour
    //-     //- h1 The Park Camper
    //-     h2= user.toUpperCase() //- use buffered javascript, it will be rendered

    //-     - const x = 9; //- unbuffered can do all your manipulatiosn/ calculations
    //-     h2= 2*x
    //-     p This is just some text 
    body
        // HEADER
        include _header
        
        //CONTENT
        block content
            h1 This is a placeholder text

        // FOOTER
        include _footer

        //- script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.21.1/axios.min.js')
        //- script(src='/js/mapbox.js')
        //- script(src='/js/login.js')
        script(src='https://js.stripe.com/v3/')
        script(src='/js/bundle.js')



